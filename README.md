## 📘 راهنمای کامل استفاده از پروژه YouTube Downloader

در این راهنما قدم‌به‌قدم یاد می‌گیرید چطور از این ابزار برای دانلود ویدیو یا صدای یوتیوب استفاده کنید. فقط کافیه یک‌بار تنظیمات اولیه رو انجام بدید، بعد از اون برای هر بار دانلود فقط چند کلیک نیاز دارید.

---

### 🧱 مرحله ۱: لاگین در گیت‌هاب

- وارد سایت [GitHub](https://github.com) بشید.
- اگر حساب کاربری ندارید، اول ثبت‌نام کنید.
- بعد با اکانت خودتون **لاگین** کنید.

> 🔐 لاگین بودن خیلی مهمه، چون بقیه مراحل فقط وقتی انجام می‌شه که وارد حساب خودتون باشید.

---

### 🔱 مرحله ۲: فورک (Fork) کردن پروژه

**یعنی چی فورک؟**  
یعنی یک کپی کامل از این پروژه رو به حساب گیت‌هاب خودتون منتقل می‌کنید.

**چجوری انجام بدید؟**

1. توی صفحه اصلی این پروژه، روی دکمه **Fork** (در بالا سمت راست) کلیک کنید.
2. توی صفحه باز شده:
   - اسم دلخواه برای ریپازیتوری جدیدتون انتخاب کنید (مثلاً `MyYouTubeDownloader`)
   - بقیه تنظیمات رو همون پیش‌فرض نگه دارید.
3. در نهایت روی دکمه سبز **Create fork** کلیک کنید.

✅ بعد از چند ثانیه، یک کپی از پروژه توی اکانت خودتون ساخته می‌شه.

---

### ⚙️ مرحله ۳: تنظیم دسترسی اکشن (Permissions)

این مرحله خیلی مهمه. اگر این کار رو نکنید، فایل دانلود شده توی ریپازیتوری شما ذخیره نمی‌شه.

**مسیر دقیق تنظیمات:**

1. توی ریپازیتوری **فورک شده خودتون** (مال خودتون، نه این یکی) وارد بشید.
2. روی تب **Settings** کلیک کنید.
3. در منوی سمت چپ، روی **Actions** کلیک کنید.
4. بعد روی **General** کلیک کنید.
5. صفحه رو پایین بیارید تا برسید به بخش **Workflow permissions**.
6. گزینه **Read and write permissions** رو انتخاب کنید.
7. در نهایت روی دکمه سبز **Save** کلیک کنید.

> 🛠️ این تنظیمات اجازه می‌دهد که فایل دانلود شده در ریپازیتوری شما آپلود شود.

---

### ▶️ مرحله ۴: فعال کردن Actions در ریپازیتوری فورک شده

وقتی شما یک ریپازیتوری رو فورک می‌کنید، گاهی اکشن‌ها به طور خودکار غیرفعال می‌شوند.

**برای فعال کردن:**

1. توی ریپازیتوری خودتون، روی تب **Actions** کلیک کنید.
2. پیامی شبیه این می‌بینید:  
   `"Workflows aren't being run on this forked repository"`
3. روی دکمه  
   **"I understand my workflows, go ahead and enable them"**  
   کلیک کنید.

✅ از این به بعد، اکشن‌های این ریپازیتوری می‌تونند اجرا بشوند.

---

### 🧠 مرحله ۵: اجرای دانلودر یوتیوب

حالا بریم سر اصل مطلب؛ دانلود کردن ویدیو یا صدا.  
**قابلیت جدید:** می‌تونید چندین لینک رو همزمان بدید تا همه با هم دانلود بشن!

1. از منوی بالای ریپازیتوری، روی تب **Actions** کلیک کنید.
2. در نوار سمت چپ، روی **🎯 YouTubeDownloader** کلیک کنید.
   > اگر این اسم رو نمی‌بینید، مطمئن بشید ریپازیتوری درست رو انتخاب کردید.
3. یک کادر آبی رنگ با عنوان **"This workflow has a workflow_dispatch event trigger"** ظاهر می‌شه.
4. سمت راست این کادر، روی دکمه **Run workflow** کلیک کنید.
5. یک پنجره باز می‌شه که باید اطلاعات زیر رو وارد کنید:

| فیلد | توضیح | مثال |
|------|-------|------|
| **URLs** | لینک(های) ویدیوی یوتیوب (با کاما، فاصله یا خط جدید جدا کنید) | `https://youtu.be/abc, https://youtu.be/xyz` |
| **Type** | نوع دانلود (`video` یا `audio`) | video |
| **Quality** | کیفیت (از منوی کشویی انتخاب کنید) | 720p |
| **Split threshold MB** | حداکثر حجم هر فایل (عدد 0 یعنی تقسیم نشه) | 90 |

> ✨ **نکته مهم درباره Quality:**  
> حالا دیگه لازم نیست کیفیت رو دستی تایپ کنید! یک **منوی کشویی** داریم که می‌تونید از بین گزینه‌های معتبر انتخاب کنید:  
> - **برای ویدیو:** `144p` | `360p` | `480p` | `720p` | `1080p`  
> - **برای صدا:** `64kbps` | `128kbps` | `192kbps` | `320kbps`

> 🔗 **چندتا لینک دارید؟**  
> می‌تونید لینک‌ها رو به سه روش وارد کنید:
> - با کاما جدا کنید: `link1, link2, link3`
> - با فاصله جدا کنید: `link1 link2 link3`
> - هر لینک در یک خط جدید (اینتر بزنید):
>   ```
>   link1
>   link2
>   link3
>   ```

6. بعد از پر کردن اطلاعات، روی دکمه سبز رنگ **Run workflow** کلیک کنید.

✅ کار شروع شد! فقط کافیه صبر کنید تا تموم بشه.

---

### ⏳ مرحله ۶: منتظر موندن برای تکمیل کار

- معمولاً این فرایند بین **۱ تا ۵ دقیقه** طول می‌کشه، بسته به طول ویدیو و کیفیت انتخابی.
- اگر چندین ویدیو داده باشید، زمان بیشتری نیاز داره (هر ویدیو حدود ۱-۳ دقیقه).
- توی همون صفحه Actions می‌تونید مراحل اجرا رو **لایو** ببینید.

#### چطور بفهمیم کار تموم شده؟

- وقتی اجرای وورکفلو تموم بشه، کنار اسمش یک **تیک سبز رنگ ✅** ظاهر می‌شه.
- اگر خطایی پیش بیاد، علامت **❌ قرمز** نشان داده می‌شه.

> 🧠 توی حین اجرا، می‌تونید روی وورکفلو کلیک کنید و خروجی مراحل رو مشاهده کنید.  
> **مراحل اصلی:** Parse URLs → Process → Download → Split → Generate Links.md → Push

---

### 📂 مرحله ۷: پیدا کردن فایل دانلود شده

بعد از سبز شدن علامت کار، فایل(های) دانلود شده در ریپازیتوری شما آپلود می‌شوند.

**مسیر دسترسی به فایل:**

1. توی ریپازیتوری خودتون، روی تب **Code** کلیک کنید.
2. پوشه **downloads** رو باز کنید.
3. فایل‌های دانلود شده رو اونجا مشاهده می‌کنید.

> 📁 معمولاً اسم فایل به این شکل هست:  
> `نام_ویدیو_آیدی_کیفیت.mp4` یا `.mp3`

**🆕 فایل Links.md:**  
توی همین پوشه اصلی ریپازیتوری، یک فایل به اسم **`Links.md`** ساخته می‌شه که توش **لینک دانلود مستقیم** همه فایل‌ها نوشته شده. می‌تونید این لینک‌ها رو با دیگران به اشتراک بگذارید یا با دانلود منیجر دانلود کنید!

> 💡 **توجه:** هر بار که دانلود جدید انجام بدید، فایل‌های قبلی پاک می‌شن و فقط فایل‌های جدید می‌مونن. پس اگر نیاز به نگه‌داشتن فایل‌های قدیمی دارید، قبل از اجرای مجدد، اون‌ها رو دانلود کنید.

---

### 🧩 مرحله ۸: اگر فایل چند پارت بود (Split شده) چیکار کنیم؟

اگر حجم فایل از مقدار `Split threshold MB` که وارد کردید بیشتر باشه، فایل به **چند پارت** تقسیم می‌شه.

مثال:  
اگر `Split threshold MB = 90` و فایل ۲۵۰ مگابایت باشه، تبدیل به سه فایل می‌شه:  
- `filename.zip`  
- `filename.z01`  
- `filename.z02`

**طریقه اکسترکت کردن (باز کردن):**

1. **همه فایل‌های پارت** رو داخل یک پوشه دانلود کنید.
2. فقط روی فایل اصلی که پسوند `.zip` داره **کلیک راست** کنید.
3. گزینه **Extract Here** یا **Extract to ...** رو انتخاب کنید.
4. فایل اصلی و کامل به دستتون میاد.

> ⚠️ نکته مهم: اگر فقط فایل `.zip` رو اکسترکت کنید، خطا می‌دهد. حتماً همه پارت‌ها در کنار هم باشند.

> 💡 **روش جایگزین (ترمینال لینوکس/مک):**
> ```bash
> zip -s- filename.zip -O combined.zip
> unzip combined.zip
> ```

---

## 🎯 خلاصه سریع (چیت‌شیت)

| مرحله | کاری که باید بکنید |
|--------|-------------------|
| ۱ | وارد گیت‌هاب بشید |
| ۲ | پروژه رو فورک کنید |
| ۳ | تنظیمات Actions رو Read/Write کنید |
| ۴ | Actions رو فعال کنید |
| ۵ | Run workflow بزنید و اطلاعات رو بدید (حالا می‌تونید چندتا لینک بدید و کیفیت رو از منو انتخاب کنید) |
| ۶ | منتظر بمونید تا سبز بشه |
| ۷ | فایل رو از پوشه downloads بردارید (یا از Links.md لینک مستقیم بگیرید) |
| ۸ | اگر چند پارت بود همه رو با هم اکسترکت کنید |

---

## ❓ پرسش‌های رایج (FAQ)

**س: چرا فایل دانلود شده در ریپازیتوری من نمیاد؟**  
ج: حتماً مرحله ۳ (تنظیم دسترسی Read/Write) رو انجام دادید؟ اون مرحله خیلی مهمه. همچنین مطمئن بشید که Actions فعال شده باشه (مرحله ۴).

**س: چقدر طول میکشه تا فایل دانلود بشه؟**  
ج: معمولاً ۱ تا ۵ دقیقه برای هر ویدیو. اگر چندتا لینک داده باشید، به تعداد ویدیوها زمان اضافه می‌شه.

**س: می‌تونم همزمان ویدیو و صدا دانلود کنم؟**  
ج: هر بار اجرا فقط یک نوع (video یا audio) می‌تونید انتخاب کنید. برای دانلود نوع دیگه، باید یک بار دیگه workflow رو اجرا کنید.

**س: فایل‌های قدیمی چی می‌شن؟**  
ج: هر بار که workflow رو اجرا می‌کنید، فایل‌های قبلی پاک می‌شن. پس قبل از دانلود جدید، فایل‌های قبلی رو ذخیره کنید.

**س: چطور لینک مستقیم دانلود رو به کسی بدم؟**  
ج: به فایل `Links.md` توی ریپازیتوری نگاه کنید. اونجا همه لینک‌های مستقیم برای دانلود وجود داره.

**س: split threshold رو چند بذارم؟**  
ج: برای فایل‌های کوچیک (زیر ۱۰۰ مگ) بذارید روی ۰. برای فایل‌های بزرگ که می‌خواید توی تلگرام یا جاهایی که محدودیت حجم دارن آپلود کنید، روی ۹۰ یا ۵۰ تنظیم کنید.

**موفق باشید! 🚀**

## ☕ حمایت مالی

اگر از این پروژه استفاده می‌کنید و براتون مفید بوده، می‌تونید از طریق لینک زیر حمایت کنید:

### 🔗 **[https://daramet.com/Tavakoli](https://daramet.com/Tavakoli)**

حتی یک حمایت کوچیک، انگیزه من برای توسعه بیشتر این نوع پروژه ها رو چند برابر می‌کنه! 🙏
